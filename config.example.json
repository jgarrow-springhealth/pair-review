{
  "_comment": "Example configuration file for pair-review. Copy to ~/.pair-review/config.json and modify as needed.",

  "_github_token_command_comment": "Shell command whose stdout is used as the GitHub token. Runs once and is cached. Default: 'gh auth token'.",
  "github_token_command": "gh auth token",
  "github_token": "ghp_your_github_personal_access_token_here",
  "port": 7247,
  "theme": "light",
  "default_provider": "claude",
  "default_model": "opus",
  "_summaries_comment": "Configures the inline natural-language hunk-summary feature. enabled gates the feature entirely; auto_generate controls whether a summary job kicks off automatically on review load (false = require a manual click on the toolbar button). provider/model fall back to default_provider/default_model and the provider's fast-tier model. max_files/max_lines_added are perf caps that skip generation for overly large diffs.",
  "summaries": {
    "enabled": false,
    "auto_generate": true,
    "provider": "",
    "model": "",
    "max_files": 50,
    "max_lines_added": 3000
  },
  "_tours_comment": "Configures the guided-tour feature. enabled gates the feature entirely; auto_generate controls whether a tour job kicks off automatically on review load (false = require a manual click on the toolbar button). provider/model fall back through summaries.provider, default_provider, and the provider's fast-tier model.",
  "tours": {
    "enabled": false,
    "auto_generate": true,
    "provider": "",
    "model": ""
  },
  "worktree_retention_days": 7,
  "dev_mode": false,
  "debug_stream": false,
  "yolo": false,
  "skip_update_notifier": false,
  "_external_comments_comment": "Opt-in feature toggle for fetching and displaying GitHub PR review comments inline. Default: false. Set to true to enable the External segment, refresh button, and /api/reviews/*/external-comments* routes.",
  "external_comments": false,
  "chat": {
    "enable_shortcuts": true,
    "enter_to_send": true
  },

  "providers": {
    "_comment": "Provider configurations override built-in defaults. Most providers have built-in models, but OpenCode and Pi require manual configuration.",

    "opencode": {
      "_comment": "OpenCode has NO built-in models - you MUST configure models here to use it.",
      "command": "opencode",
      "extra_args": ["--verbose"],
      "env": {
        "OPENCODE_TELEMETRY": "off"
      },
      "installInstructions": "Install OpenCode: curl -fsSL https://opencode.ai/install | bash",
      "default_model": "anthropic/claude-sonnet-4",
      "models": [
        {
          "id": "anthropic/claude-sonnet-4",
          "tier": "balanced",
          "name": "Claude Sonnet 4",
          "description": "Fast and capable for most reviews",
          "tagline": "Best balance of speed and quality",
          "badge": "Recommended",
          "badgeClass": "badge-recommended"
        },
        {
          "id": "anthropic/claude-opus-4",
          "tier": "premium",
          "name": "Claude Opus 4",
          "description": "Most capable model for complex reviews",
          "tagline": "Best Available",
          "badge": "Premium",
          "badgeClass": "badge-premium"
        },
        {
          "id": "anthropic/claude-haiku-3.5",
          "tier": "fast",
          "name": "Claude Haiku 3.5",
          "description": "Quick analysis for simple changes",
          "tagline": "Lightning Fast",
          "badge": "Fastest",
          "badgeClass": "badge-speed"
        },
        {
          "id": "openai/gpt-4.1",
          "tier": "thorough",
          "name": "GPT-4.1",
          "description": "OpenAI's latest model for deep analysis",
          "tagline": "Most Capable",
          "badge": "Most Thorough",
          "badgeClass": "badge-power"
        },
        {
          "id": "google/gemini-2.5-pro",
          "tier": "thorough",
          "name": "Gemini 2.5 Pro",
          "description": "Google's advanced reasoning model"
        }
      ]
    },

    "claude": {
      "_comment": "Override Claude's built-in configuration. Useful for custom paths, wrapper scripts, or additional arguments. Built-in model IDs: fable-5.1-xhigh, fable-5.1-high, fable-5-xhigh, fable-5-high, opus-5-xhigh, opus-5-high, opus-4.8-xhigh, opus-4.8-high, opus-4.7-xhigh, opus-4.7-high, opus-4.6-high, sonnet-5-xhigh, sonnet-5-high, sonnet-4.6, opus-4.6-1m, and haiku ('opus' and 'fable' are convenience aliases for opus-4.8-xhigh and fable-5-xhigh). Use 'cli_model' to decouple the app-level model ID from the CLI --model argument. Use 'env' for model-specific environment variables. A model override only replaces the fields it names — 'cli_model', 'extra_args', and 'env' fall back to the built-in definition, so renaming a built-in keeps its effort settings for both analysis and chat. Set 'supports_chat': false on a model that only makes sense for analysis (it stays in the analysis picker but is hidden from the chat model picker).",
      "_comment_default_model": "Provider-level 'default_model' selects which model is the default in the picker and when no model is specified. Preferred over the per-model 'default: true' flag (which is deprecated). If it names an unknown or disabled model, the provider falls back to automatic selection.",
      "_comment_disabled_models": "'disabled_models' hides specific models (built-in or config-defined) from the picker for this provider. A list that would hide every model is ignored. disabled_models matches by model id or alias, so either a canonical id or a convenience alias works. Example below removes the fast-tier 'haiku' model and the thorough-tier Fable 5 model (named here by its 'fable' alias).",
      "command": "claude",
      "extra_args": [],
      "env": {},
      "installInstructions": "Install Claude CLI: npm install -g @anthropic-ai/claude-code",
      "default_model": "opus",
      "disabled_models": ["haiku", "fable"],
      "models": [
        {
          "_comment": "Example: simple override — customize the display name and description of a built-in model. Note: 'tier' is required even for overrides (valid values: fast, balanced, thorough, premium).",
          "id": "sonnet",
          "tier": "balanced",
          "name": "Sonnet (Team Default)",
          "description": "Our team's standard model for reviews"
        },
        {
          "_comment": "Example: override cli_model to use a specific version string for the CLI --model flag",
          "id": "opus-4.5",
          "cli_model": "claude-opus-4-5-20251101",
          "tier": "thorough"
        },
        {
          "_comment": "Example: cli_model set to null suppresses --model entirely (model set via env instead)",
          "id": "custom-env-model",
          "cli_model": null,
          "tier": "thorough",
          "name": "Custom Env Model",
          "description": "Model specified entirely via environment variable",
          "env": {
            "ANTHROPIC_MODEL": "claude-opus-4-5-20251101"
          }
        },
        {
          "_comment": "Example: adding a custom model with extra arguments and env",
          "id": "custom-model",
          "tier": "premium",
          "name": "Custom Model",
          "description": "Example of adding a custom model with extra arguments",
          "tagline": "Custom Configuration",
          "badge": "Custom",
          "badgeClass": "badge-premium",
          "extra_args": ["--thinking-budget", "10000"],
          "env": {
            "CLAUDE_CUSTOM_FLAG": "true"
          }
        }
      ]
    },

    "antigravity": {
      "_comment": "Override Antigravity's built-in configuration. Antigravity drives the 'agy' CLI as an agentic reviewer. Built-in model IDs: gemini-3.8-flash-high (default, thorough), gemini-3.1-pro-high, gemini-3.1-pro-low, gemini-3.8-flash-low. Run 'agy models' to list everything agy exposes (Claude and GPT-OSS models included). Use 'command' for a custom path/wrapper, 'env' for environment variables, 'default_model'/'disabled_models' to shape the picker, and — on a custom model — 'cli_model' to set the exact string the 'agy --model' flag receives (an unknown name makes agy exit 1 with its model listing rather than fall back to a default).",
      "command": "agy",
      "extra_args": [],
      "env": {},
      "installInstructions": "Install the Antigravity CLI: curl -fsSL https://antigravity.google/cli/install.sh | bash",
      "default_model": "gemini-3.8-flash-high",
      "models": [
        {
          "_comment": "Example: define a custom model id backed by an exact 'agy --model' string. 'cli_model' is the display string printed by 'agy models' (a built-in Gemini string is shown here; agy also lists Claude and GPT-OSS models you can target the same way). The clean 'id' is what the UI and config keys use.",
          "id": "gemini-pro-max",
          "cli_model": "Gemini 3.1 Pro (High)",
          "tier": "thorough",
          "name": "Gemini Pro (Max effort)",
          "description": "Custom label mapped to an exact agy model string"
        }
      ]
    },

    "codex": {
      "_comment": "Override Codex's built-in configuration.",
      "command": "codex",
      "extra_args": [],
      "env": {},
      "installInstructions": "Install Codex CLI: npm install -g @openai/codex"
    },

    "muse": {
      "_comment": "Override Muse's built-in configuration. Muse drives Meta's 'muse' CLI (Muse Code) headlessly via 'muse exec --json'. Built-in model IDs: muse-spark-1.3-max, muse-spark-1.3-contributor-max, muse-spark-1.3-xhigh, muse-spark-1.3-contributor-xhigh, muse-spark-1.3-high (default; aliases 'muse-spark-1.3' and 'muse-spark'), muse-spark-1.3-contributor-high (alias 'muse-spark-1.3-contributor'), muse-spark-1.3-low, muse-spark-1.3-contributor-low. Every muse-spark-1.2-* id is an alias of its 1.3 twin (same price, better model), and the former -ultra ids alias to -max because 'ultra' is gate-closed and silently runs at xhigh. Each built-in maps to one of two real CLI models plus a --reasoning-effort level (minimal|low|medium|high|xhigh|max|ultra; 'none' is rejected). NOTE: the 'contributor' models are cheaper because Meta may use their content for product improvement; pair-review defaults to the non-contributor model so that tradeoff is always an explicit opt-in. Naming the bare CLI model 'muse-spark-1.3-contributor' resolves to the contributor variant, so it opts into that data sharing too. Run 'muse login' once to authenticate.",
      "command": "muse",
      "extra_args": [],
      "env": {},
      "installInstructions": "Install Muse Code: see Meta's Muse Code install instructions (single shell command on macOS/Linux), then run 'muse login'"
    },

    "copilot": {
      "_comment": "Override Copilot's built-in configuration.",
      "command": "copilot",
      "extra_args": [],
      "env": {},
      "installInstructions": "Install Copilot CLI: npm install -g @github/copilot-cli"
    },

    "cursor-agent": {
      "_comment": "Override Cursor Agent's built-in configuration. Uses streaming JSON output with sandbox mode enabled by default.",
      "command": "agent",
      "extra_args": [],
      "env": {},
      "installInstructions": "Install Cursor Agent CLI: https://cursor.com/docs/cli/using"
    },

    "pi": {
      "_comment": "Pi has NO built-in models - you MUST configure models here to use it. Below is just an example of that configuration format.",
      "command": "pi",
      "load_skills": true,
      "app_extensions": true,
      "extra_args": [],
      "env": {},
      "installInstructions": "Install Pi: npm install -g @mariozechner/pi-coding-agent",
      "default_model": "gemini-2.5-flash",
      "models": [
        {
          "id": "gemini-2.5-flash",
          "tier": "fast",
          "name": "Gemini 2.5 Flash",
          "description": "Google's fast model (suggested default)",
          "tagline": "Lightning Fast",
          "badge": "Fastest",
          "badgeClass": "badge-speed"
        },
        {
          "id": "anthropic/claude-sonnet-4",
          "tier": "balanced",
          "name": "Claude Sonnet 4",
          "description": "Fast and capable for most reviews",
          "tagline": "Best balance of speed and quality",
          "badge": "Recommended",
          "badgeClass": "badge-recommended"
        },
        {
          "id": "anthropic/claude-opus-4",
          "tier": "thorough",
          "name": "Claude Opus 4",
          "description": "Most capable model for complex reviews",
          "tagline": "Most Capable",
          "badge": "Most Thorough",
          "badgeClass": "badge-power"
        }
      ]
    },

    "omp": {
      "_comment": "Override OMP's (Oh My Pi, a Pi fork) built-in configuration. The built-in 'default' mode uses whatever model your OMP configuration (~/.omp/agent/config.yml) selects, so models below are optional. Model ids are passed to 'omp --model' verbatim, which fuzzy-matches ('opus', 'gpt-5.2') and accepts provider/model strings ('anthropic/claude-opus-5') — run 'omp models' to list the catalog. Set advisor to true to enable OMP's advisor runtime during reviews (disabled by default: it passively reviews each turn and injects notes, adding cost and nondeterminism).",
      "command": "omp",
      "load_skills": true,
      "advisor": false,
      "extra_args": [],
      "env": {},
      "installInstructions": "Install OMP (Oh My Pi): npm install -g @oh-my-pi/pi-coding-agent",
      "models": [
        {
          "id": "google/gemini-3.1-flash-lite",
          "tier": "fast",
          "name": "Gemini 3.1 Flash Lite",
          "description": "Google's fast model",
          "tagline": "Lightning Fast",
          "badge": "Fastest",
          "badgeClass": "badge-speed"
        },
        {
          "id": "anthropic/claude-sonnet-5",
          "tier": "balanced",
          "name": "Claude Sonnet 5",
          "description": "Fast and capable for most reviews",
          "tagline": "Best balance of speed and quality",
          "badge": "Recommended",
          "badgeClass": "badge-recommended"
        },
        {
          "id": "anthropic/claude-opus-5",
          "tier": "thorough",
          "name": "Claude Opus 5",
          "description": "Most capable model for complex reviews",
          "tagline": "Most Capable",
          "badge": "Most Thorough",
          "badgeClass": "badge-power"
        }
      ]
    },

    "my-review-tool": {
      "_comment": "Example executable provider. Runs an external CLI tool and maps its output to pair-review's suggestion format using an LLM. The tool's raw output is transformed via mapping_instructions.",
      "type": "executable",
      "command": "my-review-tool",
      "args": ["analyze"],
      "name": "My Review Tool",
      "capabilities": {
        "review_levels": false,
        "custom_instructions": false,
        "exclude_previous": false,
        "consolidation": false
      },
      "context_args": {
        "title": "--title",
        "description": "--description",
        "output_dir": "--output-dir",
        "diff_path": "--diff-path",
        "head_sha": "--sha",
        "model": "--model",
        "head_branch": "--branch"
      },
      "diff_args": [],
      "output_glob": "**/results.json",
      "mapping_instructions": "The tool outputs JSON with a 'findings' array and a 'summary' string. Each finding has: category (bug/style/perf), priority (high/medium/low), title, body, fix, path, start_line, end_line. Map path to file, body to description, fix to suggestion. Map priority to severity: high=critical, medium=medium, low=minor. Map confidence: high=0.9, medium=0.7, low=0.4.",
      "availability_command": "my-review-tool --version",
      "availability_timeout_seconds": 30,
      "_comment_availability": "availability_command is run at startup to decide if the provider is available (defaults to 'true', i.e. always available). availability_timeout_seconds bounds that check (default 10); raise it when the command runs a slow build/compile step. Also supported on chat_providers entries.",
      "env": {},
      "installInstructions": "Install my-review-tool: pip install my-review-tool",
      "default_model": "default",
      "models": [
        {
          "id": "default",
          "name": "Default",
          "tier": "thorough"
        }
      ]
    }
  },

  "chat_providers": {
    "_comment": "Chat provider command overrides. Separate from 'providers' (AI analysis). Chat providers are: pi, omp, claude, codex, copilot-acp, opencode-acp, cursor-acp. Supports 'command', 'args' (replaces defaults), 'extra_args' (appends), 'env', 'load_skills' (Pi/OMP: false suppresses skill auto-discovery), and 'app_extensions' (Pi: false omits pair-review task extension; OMP never loads it) overrides. Codex also supports 'sandbox' ('workspace-write' or 'read-only'). 'availability_command' overrides the startup availability probe and 'availability_timeout_seconds' bounds it (default 10; raise for slow build commands). 'model' sets the default model the chat picker starts on for that provider (a catalog id, alias, or raw CLI model string, passed through verbatim if unrecognized). 'models_from' picks which review provider's model catalog (from 'providers' above) feeds the chat picker; built-ins map automatically (copilot-acp->copilot, opencode-acp->opencode, cursor-acp->cursor-agent, others use their own id), and a config-defined chat provider falls back to its 'type'.",
    "claude": {
      "_comment": "Example: use a wrapper script for Claude chat, and default new conversations to the opus-5-high effort tier",
      "command": "claude",
      "model": "opus-5-high"
    },
    "codex": {
      "_comment": "Example: use a wrapper script for Codex chat. Use sandbox='read-only' for discussion-only sessions.",
      "command": "codex",
      "sandbox": "workspace-write"
    }
  },

  "repos": {
    "_comment": "Repository configurations (renamed from 'monorepos'; the old key still works). 'path' points to an existing local clone. 'checkout_script' runs after worktree creation. 'reset_script' runs when switching a pool worktree to a new PR. 'load_skills' controls whether AI providers load environment-level skills (default: true). 'skip_bulk_fetch' disables the periodic background fetch over pool worktrees (default: false); foreground worktree creation and refresh always use targeted ref fetches, so they are unaffected. Useful for very large monorepos where bulk fetching all refs is prohibitively slow. 'fetch_timeout_seconds' governs the background pool fetch: it is that `git fetch`'s idle timeout — git is killed after this long with no output (default: 300). Raise it for monorepos whose delta resolution is silent for minutes. Foreground worktree creation and refresh use targeted ref fetches and are not governed by it.",
    "owner/repo": {
      "path": "~/path/to/monorepo",
      "checkout_script": "my-repo-checkout $BASE_SHA $HEAD_BRANCH",
      "reset_script": "my-reset-tool --base \"$BASE_SHA\" -- \"$HEAD_BRANCH\"",
      "checkout_timeout_seconds": 300,
      "fetch_timeout_seconds": 300,
      "worktree_directory": "~/custom/worktrees",
      "worktree_name_template": "{id}/src",
      "pool_size": 0,
      "pool_fetch_interval_minutes": null,
      "load_skills": true,
      "skip_bulk_fetch": false
    }
  }
}
